import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFFFF6B11); //#FF6B11
  static const Color callBackground = Color(0xFF4589FF);

  // Status colors
  static const Color newContact = Color(0xFFFF6B11);
  static const Color existingContact = Colors.grey;

  // Text colors
  static const Color phoneNumber = Color(0xFFFF6B11);
  static const Color dateText = Colors.black87;
  static const Color originText = Colors.grey;

  // Background colors
  static const Color callIcon = Colors.white;
}
